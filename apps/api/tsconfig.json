{
  "extends": "@sit-igp/tsconfig/nestjs.json",
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "dist", "build", "coverage", ".next", ".turbo"],
  "compilerOptions": {
    // Modules
    "baseUrl": ".",

    // Emit
    "outDir": "./dist"
  }
}
